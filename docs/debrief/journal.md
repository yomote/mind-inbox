# デブリーフ journal

> `design-gate` / `debrief` セッションの記録。仕組みは [ADR 0014](../adr/0014-design-comprehension-gate-and-debrief.md)。
>
> 役割は 2 つ: (1) `debrief` skill の「前回以降」の**起点マーカー** (最新エントリの日付を使う)、(2) user の決定と学びの**累積ログ** (解説の深さ調整・振り返りに使う)。

## エントリ形式

新しいエントリを**このセクションの直後 (ファイル上部)** に追記する:

```markdown
## YYYY-MM-DD — {design-gate | debrief}

- **対象**: {扱った設計 / PR / ADR の一覧}
- **決定**: {ADR NNNN Accept / Reject / 修正、design-gate の承認・修正点}
- **学びメモ**: {user が曖昧だった箇所 → どう解説し直したか。次回の解説の深さ調整に使う}
- **特記**: {任意。そのセッション固有の記録 — 例: agent 側の失敗・見落としの分析。無ければ省略してよい}
- **持ち越し**: {未消化の項目・次回に回した判断。無ければ「なし」}
```

必須は **対象 / 決定 / 学びメモ / 持ち越し** の 4 項目。`特記` は任意項目で、省略しても他のエントリと同じ形として扱う。

---

## 2026-08-08 — briefing (#2, 保守性リファクタリング)

- **対象**: 外部 AI レビュー (「上物過多・本体薄い」) を受けた保守性調査 / epic #135 + sub-issue 8 件 / Phase 1 実装 (PR #145: docs 誤情報修正・型ハブ分離・BFF 依存一方向化)。資料: briefing #2 artifact (音声はブラウザ読み上げ — 実行環境で docker 不可のため VOICEVOX 断念)。※同日午前の briefing #1 (別セッション) と番号衝突したため #2 に改番
- **決定**: 判断事項 5 件をすべて回答 (#143 close) — ①approve フローは MAF 移行 (#81) まで現状維持 ②相談タイトルは 26 文字 (BFF 値) に統一 (#142 へ) ③stub 応答の UI 警告を出す (#146 起票) ④jsdom + testing-library を導入し Phase 3 はテストファースト (#141 へ) ⑤`dialogue session.mdx` はリネーム (#140 へ)。加えて Phase 1 の PR #145 マージと Phase 2 着手を PO が承認
- **学びメモ**: PO から逆提起 2 件 — (1)「テスト不能な構成が設計悪化の原因では (TDD)」: リポジトリ内の相関 (L2 が効く bff / DI+テストの ai-agent は構造が良く、テスト物理不可の frontend に状態が集中) がこれを裏付け。「テスト可能性を設計基準に昇格」の方針で反映。(2)「docs は必ず陳腐化する — 定期リファクタチェック機構が欲しい」→ #147 起票、週次 Routine は MCP 承認不達のため PO が UI から登録 (初回発火 2026-08-10 09:00 JST で実確認)
- **特記**: PR #145 のレビュー major (CI red) は isolate:false 下の module registry 汚染 (順序依存 flaky) と特定し `vi.resetModules()` で修正。実装セッションからは create_trigger / create_session / send_later が承認ゲートで全滅 → needs-human 1 クリック宿題方式 (#90/#92 と同型) に切替
- **持ち越し**: Phase 3 (#141/#142) は PR #132 着地待ち / #147 は初回発火確認後にチューニングして close / Proposed ADR 0025 は次回 debrief

## 2026-08-08 — briefing (#1, hub-and-spoke 初日の全成果)

- **対象**: 音声ナレーション付きスライド 10 枚 (`briefing` skill の初実戦)。① 実環境の「緑なのに嘘」6 連発の根治 (#115/#118/#113/#117/#125/#126) ② 持ち越し解説 = UI 入口の迷走 (#103→#110) と MAF 等価移行の第一歩 (#85) ③ PM ハブ運用の初日実績 (ADR 0021) ④ UX 課題の 4 分解とコスト結論 (#119〜#122) ⑤ 承認 2 件
- **決定**: **ADR 0022 (UX 自律改善ループ) / ADR 0023 (Azure Speech F0) をともに Accept**。あわせて PR #128 (UX プローブ + judge) / #129 (briefing skill) / #130 (音声入力) をマージ。PO 評価「かなり良い。筋がいい。世の中に共有してもいいぐらい」
- **学びメモ**: 質疑ゼロで通過。**形式そのものが効いた** — 「読む負担をエージェントへ移す」設計 (説明し切る + 聞き流せる音声) が、debrief #3 で残った「理解しきれた気がしない」への直接の答えになった。PO からの改善要望は速度のみ (1.12 倍 → **1.5 倍が聞きやすい**)。次回以降 1.5 倍を既定にする
- **特記 (agent の失敗)**: ①VOICEVOX の docker イメージが初回はネットワークポリシーで取得できず、ブラウザ読み上げにフォールバック → PO が設定を開けてくれて途中からずんだもんに切替 (Issue #134)。②音声プレイヤーを差し替える際、正規表現置換で**スライド送り関数 `show()` ごと削除**してしまい、ページングが壊れた。PO の指摘で発覚。教訓: 生成物の JS は置換後に構文・機能の両方を確認する (skill に反映済み)。③この日は API 使用量上限に 2 回当たり、子エージェントが計 5 回中断 — WIP の push で毎回退避できたため損失ゼロ
- **持ち越し**: ADR 0024 (SSE ストリーミング方式) は本会で未提示のため次回に承認を持ち越し / rubric 初版の PO レビュー + M1.5 較正実験 / デプロイ後の実機マイクテスト (1〜2 分発話) / needs-human 残 2 件 (#92 #90)

## 2026-08-08 — debrief (#3, 認証の門 / リリースゲート / 検証ループ)

- **対象**: 前回以降のマージ 14 本のうち重要 3 テーマ — ①認証の門の完遂 (#100/#101/#104/#111, ADR 0017 実装。IP 許可リスト方式の死亡確認と TTS 結線バグ根治を含む) ②リリースゲート基盤 (#105, ADR 0019/0020。テストランで入口崩壊を検出) ③検証ループ (#95/#97/#99/#111/#113, ADR 0018。ゴールデンパス常設化と msal キャッシュ注入 → プロトコル偽装の顛末)。可視化資料: debrief #3 artifact
- **決定**: **ADR 0018 / 0019 / 0020 / 0021 をすべて Accept** (Proposed キュー 0 に)。0021 は PR #114 でマージまで実施。※当初 0014 も承認対象に挙げたが、本文中の文字列に grep が誤ヒットしたもので debrief #1 で Accepted 済みだった
- **学びメモ**: 理解確認 3 問中 2 問正解。IP 許可リストの死因 (実送信元 IP が公表リスト外になりうる) と judge 分離の理由 (実装文脈のバイアス遮断) は正確に理解。**「プロトコル偽装方式が壊れにくい理由」を「Playwright の公式機能だから」と回答** → 道具の公式性ではなく依存先の契約の違い (ライブラリ内部形式 = 破ってよい約束 vs OAuth プロトコル = 破れない約束) と解説し直した。前回の「値の正しさより管理の担当」と同系で、**原因や根拠を表層 (見えている道具・値) に求める傾向**がある。今後は「何に依存しているか / 誰が何を約束しているか」を先に図で示す
- **特記**: 本日から hub-and-spoke 運用開始 (ADR 0021)。この debrief 自体を親 [PM] セッションが実施し、並行して子 2 本 (#107 デプロイ no-op / #112 認証 UX) が稼働中 — 規約どおり親は docs のみ触り、開発は子に分配した
- **持ち越し**: 次回 debrief テーマ = UI 入口の迷走と決着 (#103→#110) / v2 M1 の第一歩 (#85 SK→MAF)。needs-human 残: #92 (受信箱 Routine) / #90 (release-gate Routine) / #112 (子セッション対応中)

## 2026-08-07 — po-feedback (初回)

- **対象**: レビューエージェント基盤の構築セッション (`claude/security-review-agent-9avoqw`、ADR 0019 一式)
- **👍**: 問題を incentive 構造で設定 (「実装者はリリース側に流れる」) / 段階的具体化でちゃぶ台返しゼロ / 「毎回走ると無駄」のコスト感覚
- **👎**: 音声由来の曖昧語 (「東京だったら」「衛生的な機能」等) にエージェントが解釈を明示したが**未応答のまま実装が進んだ** / 1 クリック宿題の滞留 (ブランチ保護が未設定 = ゲートの強制力がまだ無い) / スコープ宣言の不在 (release-judge R0 は PO の「入るもの/入らないもの」宣言がないと UNKNOWN が並ぶ)
- **📌 次の期間で 1 つだけ**: エージェントの「〜と解釈しました」に、合ってる/違うの**一言だけ**返す
- **持ち越し**: ブランチ保護の設定、ADR 0019 (旧番号 0015 → main の採番と衝突したため 0019 に改番) の Accept/Reject、リリース PR 運用の初回実践
- **記録**: [release-gate テストラン全記録 (🔴 NO-GO、4 レポート + 作業指示)](archive/release-gate-testrun-2026-08-07.md)

## 2026-08-07 — design-gate (#86 Container Apps の第二の扉)

- **対象**: Issue #86 の恒久対策方式。ADR 0017 を起案・承認
- **決定**: **Option A (認証の門)** — ai-agent/vv-wrap に CA 組み込み認証 + BFF が Managed Identity トークン、voicevox は internal ingress 化。VNet 案は不採用 (「将来やる」予約もしない — 必要時に新 ADR)
- **学びメモ**: user は当初 VNet 案 (ネットワークで閉じるのが原則として正しい) に傾いた → 現構成の Functions が Y1 で VNet 統合非対応であり、EP1 だと予算 (月¥3,000) 即死 / Flex Consumption 移行だと工事最大、という「正しさの通行料」を可視化したら判断が変わった。原則と通行料を並べて見せる説明は今後も有効。教訓「守るべき資源から到達経路を全部数える」をゲート観点に追加
- **持ち越し**: #86 の実装 (IaC 反映 + smoke-test に無認可 401 実測)。実装は承認済み設計の範囲内なのでゲート不要

## 2026-08-07 — debrief (#2, epic #70 常設 dev 環境)

- **対象**: epic #70 のマージ済み PR 6 本 — #75 (SWA Free + 認可を Functions へ移設 + 予算) / #76 (CAE 3→1) / #77 (自動デプロイ結線 + 夜間 teardown 廃止) / #78 (認可パラメータ固定) / #87 (EasyAuth から /admin/\* 除外) / #88 (偽の失敗で止めない + smoke-test を CD に組込)。可視化資料: debrief #2 artifact
- **決定**: **ADR の裁定なし** (Proposed キューは 0 件。0013 は design-gate #69 と debrief #1 で承認済み、0009 は Superseded by 0013 に更新済み)
- **学びメモ**: 理解確認 2 問とも設計と違う理解 → 再解説した:
  - **(1) 「voicevox の IP 制限だけ 3 回消えたのはなぜか」→ 「設定した IP が間違っていたから」と回答。** 実際は _正しい egress IP に直した 2 回目も消えている_ ので IP の正誤は原因ではない。真因は **「誰がそのリソースを宣言しているか」**: voicevox は bicep が ingress ブロックごと宣言しているため再適用で宣言外の設定が消える / ai-agent・vv-wrapper は bicep に存在せず `az containerapp update` が対象を絞って更新するため手動設定が生き残る。**宣言的管理の外で加えた変更は一時的**、という IaC の一般則として解説し直した。user は「値の正しさ」に原因を求める傾向があるので、今後は **「管理の担当（誰が宣言しているか）」を先に図で示す**
  - **(2) 「CD が赤いとき最初にやることは」→ 「エラーメッセージを検索して原因を特定する」と回答。** 一般には正しいが、今回はそれで **agent 自身が一度誤診している** (Bad Request を EasyAuth のせいと診断し PR #87 を費やしたが CD は赤のまま。真因は config-zip の _事後チェック_ だけの失敗)。解決したのは実態確認 (`WEBSITE_RUN_FROM_PACKAGE` の更新時刻 / `function list`)。**エラーメッセージは「何が起きたか」ではなく「何を検知したか」しか語らない**、CLI の終了コードは操作でなく事後チェックを反映しうる、と解説。順番は「まず実態 → それからメッセージ」
- **特記 (agent の失敗の記録)**: 設計も PR レビューも通っていたのに、**実環境に適用した瞬間に 6 件の「壊れているのに緑のまま通る」問題**が出た。うち 3 件は出荷寸前 — ①認可パラメータ未固定で _認証無効フロント_ を出荷しかけた ②OpenAI の鍵を持つ ai-agent が実際に無認可公開されていた (門を 1 枚足したのに、守るべき資源への別経路を数えていなかった) ③`staticIp` を egress と誤認した IaC で TTS を壊しかけた (レビューが阻止)。さらに **再発防止の smoke-test を書いた PR #88 で同種のバグを 6 個作った** (パス依存 / 未検証を ok と偽る / 無言スキップ / 対象漏れ / 参照先 output が常に空 / stderr 混入)。「終了コードを鵜呑みにしない」がテーマの PR で終了コード 0 の中身を鵜呑みにしていた
- **持ち越し**:
  - `deploy-frontend.sh` が旧 SWA 認証の app settings (`AZURE_CLIENT_ID`/`SECRET`) を必須にしたままでフロント未配置 (#69 の積み残し)
  - **#86 の design-gate** — 3 つの Container App をどう守るか (IP 許可リスト / 内部 ingress + VNet / Container Apps 側 EasyAuth)。**「誰が宣言するか」を込みで決める**こと。現状の IP 制限は手動・IaC 外で再デプロイのたびに剥がれる
  - `AUTO_DEPLOY_ENABLED=true` の解禁 (認可設定と 401 実測の完了が前提。GitHub Variables API は agent から触れないので user 操作)
  - CORS preflight が EasyAuth に巻き込まれるリスクの実測 (smoke-test に判定は入れたが、フロント未配置のため未通過)

## 2026-08-06 — design-gate (#69 EasyAuth / SWA Free 化)

- **対象**: issue #69「SWA Free 化 + Functions EasyAuth(Entra 自分限定) + CORS + 予算アラート」。根拠 ADR: [0013](../adr/0013-standing-low-cost-dev-env-with-auto-deploy.md)
- **決定**:
  - **承認** — この設計で実装に進む。あわせて ADR 0013 を `Proposed` → `Accepted`、ADR 0009 を `Superseded by 0013` に更新
  - 「自分限定」の強さ: **A. 単一テナント限定のみ**（テナントに実質本人しか居ないため、設定が最小で壊れにくい方を採る）
  - 予算アラート: **月 ¥3,000**、通知先はアカウントのメール。actual 50% / forecast 80% / actual 100% の 3 段
  - ローカル開発: **認証なし**（Functions Core Tools に EasyAuth が無く、ローカル BFF は自機内のみ）
- **学びメモ**: 理解確認で「第三者が Functions を直接 curl したら?」に対し **「CORS がブロックするので届かない」** を選択 → **CORS はブラウザ側の規約であって認可ではない**（curl/Postman はヘッダを無視して到達する。CORS の役割は "他人のサイトの JS がログイン中のブラウザを踏み台にするのを防ぐ" こと）を解説し直した。実際に止めるのは EasyAuth の 401。次回この領域を扱うときは「守りの層がどこにあるか」を先に図で固定してから細部に入る
- **持ち越し**: EasyAuth 有効時に **CORS preflight (OPTIONS) が 401 になる既知リスク**の実測。実デプロイ時に未認証 401 とあわせて確認し、preflight が弾かれる場合は `globalValidation.excludedPaths` などの回避を runbook に追記する

## 2026-08-06 — debrief (#1 続き・完了)

- **対象**: 持ち越し分 — v1 本流ゼミの逆質問 Q1〜Q4 + Proposed ADR 0010/0011/0013/0014 の裁定
- **決定**: 0010 **Accept** / 0011 **Accept** / 0013 **Accept** / 0014 **Accept** (Proposed キュー消化、残 0 件)
- **学びメモ**: 理解確認 4 問中 2 問で設計と違う理解 → 再解説した: (1) 再出現しても status は自動で open に戻らない (自動化したのはグルーピングのみ。状態遷移は user の編集権 = トリアージに残す、ADR 0007)。(2) 壊れた LLM JSON は 500 でなく空結果 (吐き出しを失わない優先。fail loudly より体験保護)。user は「状態は自動遷移する」「エラーは 500」という直感を持つので、"自動化の境界" と "静かなフォールバック" は今後も明示して解説する
- **持ち越し**: なし。次アクション = ADR 0015/0016 に基づく M1 (MAF 等価移行) の実装計画

## 2026-08-06 — design-gate (ADR 0015/0016 能動化+MAF)

- **対象**: エージェント・ワークフロー再設計 — ADR 0015 (プロアクティブ解禁の思想転換) + ADR 0016 (SK → MAF 移行)。可視化資料: design-gate #1 artifact
- **決定**: 0015 **Accept** / 0016 **Accept**。修正 1 件 — G2 を「受信箱で待つ」から「**プッシュ既定 + 頻度予算・静穏時間・カテゴリ別設定**」へ user 判断で変更 (通知は受信箱にも残る)。M2 の課金リソース (索引) と外部 write 連携は着手前に再ゲート
- **学びメモ**: M1 等価移行の意義が「依存関係」止まりだった → 「変化を 1 度に 1 種類にしてリスクを分離する (契約不変 + OpenAPI diff が移行の検知器になる)」を再解説。「統合ミスをどう戻すか」→ Mention 不変 (ADR 0007) + `relink` の対応を再解説。次回もリスク分離系の話は具体例から入る
- **持ち越し**: debrief #1 が途中 (v1 本流ゼミの逆質問 Q1〜Q3 未消化 / Proposed ADR 0010・0011・0013・0014 が未裁定のまま)。次回 debrief で裁定する

## 2026-08-06 — debrief (#1, 途中まで)

- **対象**: v1 本流 Phase A→B→C (PR #57/#58/#60/#65/#71)。ゼミ資料 artifact を作成・解説まで実施
- **決定**: なし (逆質問の途中で MAF の設計相談に移行し、design-gate #1 へ)
- **学びメモ**: —
- **持ち越し**: 逆質問 Q1〜Q3 / Proposed ADR 0010・0011・0013・0014 の Accept/Reject
