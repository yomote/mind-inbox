import { HomeScreen } from "../../components/screens/HomeScreen";

export function HomeSpecPreview() {
  return (
    <HomeScreen
      onNewConsultation={() => {}}
      onHistory={() => {}}
      onSpecPreview={() => {}}
    />
  );
}
