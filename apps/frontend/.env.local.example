# mock/real 切り替え。true なら mockApi、false なら BFF tRPC を使う。
VITE_USE_MOCK=false

# 任意。dev 時に Vite proxy を経由せず BFF を直接叩きたい場合に設定する。
# 通常は未設定のままで /api を Vite proxy が localhost:7071 に転送する。
# VITE_BFF_BASE_URL=http://localhost:7071

# VOICEVOX 話者 ID（任意、デフォルト 3）
VITE_VOICEVOX_SPEAKER=3
