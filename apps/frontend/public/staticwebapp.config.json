{
  "$comment": "SWA Free (#69 / ADR 0013): SWA は静的ファイルを配るだけ。認可は BFF(Functions) の EasyAuth が担うため、ここでは認証を要求しない。SWA に認証を掛けても別オリジンの API は守れないので、両方に門を置くとログインが二重になるだけで守りは増えない。",
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/assets/*", "*.{png,jpg,jpeg,svg,ico,webp,woff,woff2}"]
  },
  "globalHeaders": {
    "X-Content-Type-Options": "nosniff",
    "Referrer-Policy": "strict-origin-when-cross-origin"
  },
  "mimeTypes": {
    ".json": "application/json"
  }
}
