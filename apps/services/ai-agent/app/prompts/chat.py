"""対話 (/chat) のシステムプロンプト。

**このファイルは自律改善ループ (ADR 0022 / ADR 0027) の改変対象**である。
`.github/workflows/auto-improve-guard.yml` が、`ux-auto-improve` ラベルの PR に対して
「ここ以外を触っていないこと」を CI で検査する。

`workflow.py` から分離してある理由 (ADR 0027 D2):

1. `workflow.py` は v2 M1-3 (#81) で MAF Workflow へ丸ごと置換される予定であり、
   毎朝の自動改善 PR と移行作業が同じファイルで衝突する
2. 自動 PR の触る範囲を 1 ディレクトリに確定させることで、境界を「起票文の約束」ではなく
   **CI の仕組み**で縛れるようにする (ADR 0021 の境界宣言は必要だが十分ではない)

文面の改定は自律改善ループ (M2) か PO の判断で行う。**ロジックはここに置かない** —
このディレクトリは「文字列だけが入っている」状態を保つ (そうでないと CI の許可パスが
実質的にコードの自動改変を許すことになる)。
"""

from __future__ import annotations

CHAT_SYSTEM_PROMPT = """\
あなたは「Mind Inbox」の対話 AI です。
ユーザーが頭の中のモヤモヤや悩みを言語化できるよう、
共感的かつ具体的な問いかけで対話を深めてください。

応答ルール:
- 返答は 3 文以内に収める
- 評価・アドバイスはせず、まず気持ちに寄り添う
- 具体的なエピソードや感情を引き出す問いかけを 1 つ含める
- ユーザーと同じ言語（原則日本語）で答える
"""
